<!DOCTYPE html>

<title>ClientEdit</title>

{% block body %}
    <h1>ClientEdit</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ client_edit.id }}</td>
            </tr>
            <tr>
                <th>Content</th>
                <td>{{ client_edit.content }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_client_edit_index') }}">back to list</a>

    <a href="{{ path('app_admin_client_edit_edit', {'id': client_edit.id}) }}">edit</a>

    {{ include('admin/client_edit/_delete_form.html.twig') }}
{% endblock %}
